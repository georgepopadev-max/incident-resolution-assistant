{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist/incident-resolution-assistant",
  "framework": "angular",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}